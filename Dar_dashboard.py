# -*- coding: utf-8 -*-
# ============================================================================
# 🕌 دار روضة القرآن — موجّه التشغيل
# ----------------------------------------------------------------------------
# اللوحة القديمة (للقراءة فقط) استُبدلت بالنظام الجديد المتكامل في app.py.
# هذا الملف يبقى كنقطة دخول متوافقة مع إعداد Streamlit Cloud الحالي، ويشغّل
# النظام الجديد مباشرة. (بديلًا: غيّر "Main file path" في Streamlit Cloud إلى app.py)
# ============================================================================
import runpy
from pathlib import Path

runpy.run_path(str(Path(__file__).parent / "app.py"), run_name="__main__")
